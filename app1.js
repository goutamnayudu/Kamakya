function search() {
    var input = document.getElementById("searchInput").value;
    // Perform search operations here, for example, you can display search results below the search bar
    document.getElementById("searchResults").innerHTML = input;
  }
  